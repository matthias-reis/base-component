# CurrentTask: Work on Feedback for Implementation

- **ID**: `#<%= it.issue.number %>`
- **Title**: `<%= it.issue.title %>`
- **Workpackage Name**: `<%= it.workPackageName %>`

## Task Description

- A detailed plan has already been worked out and implemented, see `<%= it.workPackageName %>/PLAN.md` for more details. But there is feedback to address.
- Read the text below, especially the "Feedback Comments" and the "CI Status" section.
- Ensure that all relevant feedback is addressed in the codebase.
- Do **not** modify the plan.md as this has been previously approved.
- Append a line to `<%= it.workPackageName %>/cost.md` with:
  - timestamp (UTC), provider, model, input_tokens, output_tokens, total_tokens, estUSD, headers snapshot (remaining/reset if available).
- create a local commit if possible. If not propose a commit statement including message.

<%~ include("./comments.eta", it) %>

<% if (it.checks && it.checks.length > 0) { %>
## CI Status

<% it.checks.forEach(check => { %>
__<%= check.name %>__: <%= check.conclusion === 'failure' ? '❌' : '✅' %>

<% }) %>
<% } %>

## Pull Request Information

- **PR ID**: `#<%= it.pullRequest.number %>`
- **Title**: `<%= it.pullRequest.title %>`
- **State**: `<%= it.pullRequest.state %>`
